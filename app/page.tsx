// Minimal Next.js App Router skeleton — the server-side seam Track 4 builds on.
// The AI provider key is handled server-side only and never reaches this client
// surface. Intentionally bare until the input form lands in Track 4.
export default function Home() {
  return (
    <main>
      <h1>WordPress Block Theme Generator</h1>
      <p>Describe your site and download an installable Block Theme.</p>
    </main>
  );
}
