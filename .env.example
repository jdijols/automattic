# Anthropic API key for the default AI provider.
# Server-side only — this value must never be exposed to the client bundle.
# Copy this file to `.env.local` and fill in your key for local development.
ANTHROPIC_API_KEY=
