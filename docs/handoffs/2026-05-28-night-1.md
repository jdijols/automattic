---
session: night-1
date: 2026-05-28
branch: main
status: phase-1-planned-reviewed · phase-2-research-loop-running
---

# Handoff — Night 1

Durable narrative for resuming in a fresh chat. Pairs with the `/context-save` checkpoint (machine-local auto-resume). This doc references artifacts by path — it does **not** duplicate them. Read the linked files for detail.

## Where we are

Set up the project from scratch and completed the **Plan stage** for Phase 1 using the autonomous-research-loop recipe. Phase 1 PRD is written, independently reviewed, hardened, and committed. A **Phase 2 research loop is running overnight**. No application code exists yet — everything so far is grounding + planning artifacts.

## Key decisions (links, not restatements)

- **What we're building:** AI-powered WordPress FSE Block Theme generator. Hard constraint: zero Custom HTML block. → [`CLAUDE.md`](../../CLAUDE.md), [`STRATEGY.md`](../../STRATEGY.md)
- **Product strategy** (problem, approach, 4 tracks, metrics, contractual invariant): [`STRATEGY.md`](../../STRATEGY.md)
- **Stack:** Node/TS + Next.js App Router + Vercel AI SDK (`generateObject`+Zod, default Claude, swappable) + AJV + Vitest + Playwright + JSZip + WordPress Playground harness + npm. → [`docs/adr/0001-application-stack.md`](../adr/0001-application-stack.md)
- **Phase 1 design** (IR schema, validator, assembler, install harness): [`docs/phase-1-prd.md`](../phase-1-prd.md). Q1 resolved → **hybrid IR** (pattern-reference + constrained free-tree). 8/10 architectural questions resolved.
- **Track 2 seed** (the taste backbone): 3 validated patterns + the IR forcing-function notes. → [`docs/pattern-library/`](../pattern-library/)
- **Workflow:** autonomous research loop for the *Plan* stage; **Gated handoff** (human at the gate) for *Build* + *Test*. Autonomous build is NOT used (no trust layer yet).
- **Naming:** repo is `Automattic` (the partner company); a real product name is a TODO before deploy. → [`CLAUDE.md` § Naming](../../CLAUDE.md)

## Still open (must address before/during `/ce-plan`)

Three strategic calls deferred by the independent doc-review — see the **Deferred / Open Questions** section at the end of [`docs/phase-1-prd.md`](../phase-1-prd.md):
1. **Effort allocation** — Track 1 correctness depth vs. the distinctiveness axis the brief rewards (Track 2 is still a 3-pattern seed).
2. **Allowlist distinctiveness ceiling** — pre-admit `media-text`/`gallery` etc. vs. keep the allowlist pattern-gated.
3. **"Frozen while Q7 open"** — qualify the freeze (envelope frozen, recursive-tree encoding provisional) vs. measure Q7 first.

Plus two cross-track architectural questions still genuinely open in the PRD: **Q7** (recursive IR vs. hosted structured-output limits — *being resolved by the Phase 2 loop right now*) and **Q9** (WP-version coverage).

## Tomorrow — exact next steps

1. **Check the Phase 2 loop output:** [`docs/phase-2-prd.md`](../phase-2-prd.md). Did it converge? Is **Q7 resolved** with an experiment design? Any `CONTRACT-CHANGE-REQUEST` blocks (would mean Phase 1's IR shape needs revisiting)?
2. **Commit the Phase 2 PRD** — the loop writes but does not commit. (`docs: add Phase 2 PRD …`)
3. **Resolve the 3 deferred strategic questions** above (5-min taste calls, with you).
4. **`/ce-plan docs/phase-1-prd.md` — WITH you** (it's Gated-handoff; the `/autoplan` + `/grill-with-docs` taste gates want your eye). Output: buildable tickets. → then `/to-issues`.
5. **Scaffold + build Phase 1** (TDD-driven, you at the gate): `npm init`, Next.js + Zod + AJV + Vitest per ADR-0001, then `/tdd → /ce-work → verify → /code-review → /ship` per ticket.

## Gotchas / notes for the next session

- **gbrain embeddings are empty.** `VOYAGE_API_KEY` is set in the shell but the prior `/sync-gbrain` runs happened before it was set, so 0 chunks embedded. **Fix:** run `/sync-gbrain --full` in a *fresh* session (sources the key) to backfill — then semantic `gbrain search` works.
- **Don't edit `docs/phase-1-prd.md` or the seed files** while the Phase 2 loop is running (it re-reads the frozen contract each iteration).
- **Two Opus loops ran on the meter** (Phase 1 + Phase 2). Check usage when fresh.
- **`References/Bookmarks.md`** has an uncommitted edit (your link with a text/href glitch) — left unstaged intentionally.
- gstack is on **v1.51.0.0**. gbrain is local PGLite (`mode: local-stdio`).
- `mcp__gbrain__*` tools require a Claude Code restart to appear (loaded at session start).

## Suggested skills for next session

- `/context-restore` — or just invoke any gstack skill; the preamble auto-surfaces the latest checkpoint.
- `/sync-gbrain --full` — backfill embeddings (see gotcha above).
- `/ce-plan docs/phase-1-prd.md` — the next real step (with you).

## Commit trail (the story so far)

`git log --oneline` on `main`: foundation → gbrain config → naming fix → STRATEGY.md → Phase 1 seed → ADR-0001 → pattern library (+validation findings) → Phase 1 PRD → doc-review fixes → gitignore chore → Phase 2 seed.
