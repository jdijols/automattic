{ "description": "version:2 against the v3-pinned schema is rejected (the version-pair trap)", "target": "themejson", "expect": "reject", "expectedCode": "THEME_JSON_INVALID", "expectedInvariant": "invalid-theme-json",
  "input": { "version": 2, "settings": {} } }
